<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Fond noir posé dès le thème système, et pas seulement dans Compose : sinon un flash
         blanc apparaît entre l'affichage de la fenêtre et la première composition. -->
    <style name="Theme.TafMetar" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
